{
  "name": "Doctor-Appointment-App-NextJS",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev:web": "yarn workspace web dev",
    "dev:api": "yarn workspace api dev",
    "dev": "concurrently -n WEB,API -c auto \"yarn dev:web\" \"yarn dev:api\"",
    "build": "yarn workspace @starter/shared build && yarn workspaces foreach -p -A run build",
    "start": "concurrently -n WEB,API -c auto \"yarn workspace web start\" \"yarn workspace api start\""
  }
}
